
{
  "name": "scrapit",
  "version": "1.0.0",
  "description": "ScrapIt— Recycling Pickup Scheduling Platform",
  "private": true,

  "repository": {
    "type": "git",
    "url": "git+https://github.com/Yaas-meen/ScrapIt..git"
  },

  "bugs": {
    "url": "https://github.com/Yaas-meen/ScrapIt./issues"
  },

  "homepage": "https://github.com/Yaas-meen/ScrapIt.#readme",

  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "build": "npm --prefix client run build"
  },

  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}